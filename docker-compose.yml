services:
  frontend:
    image: cicd/frontend:${COMMIT_HASH}
    container_name: cicd-frontend
    build:
      context: .
    ports:
      - 5173:80
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped